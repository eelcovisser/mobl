application twittertest

title "Twitter test"
database "twitter"

import mobl

screen root() {
  header("Zef's twitter updates")
  list(update : twitter::TwitterUpdate in twitter::getUpdates("zef")) {
    listitem { outputString(update.text) }
  }
}